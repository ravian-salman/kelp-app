import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  balance: {
    color: '#fff',
    fontSize: 50,
    fontWeight: '700',
  },
  precision: {
    color: '#fff',
    fontSize: 24,
    fontWeight: '600',
    paddingBottom: 40,
  },
});

export default styles;
